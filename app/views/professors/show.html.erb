<section>
  <p>Average Age: <%= @professor.students_average_age.round(1) %></p>
  <% @professor.students_alphabetical.each do |student| %>
    <article class="student-<%= student.id %>-details">
      <%= link_to "#{student.name}", "#" %>
    </article>
  <% end %>
</section>
